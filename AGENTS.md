# AstroArabia

- Arabic-first and RTL by default.
- Use logical CSS properties such as `start`, `end`, `ms`, and `me`.
- Keep code blocks and technical values explicitly LTR.
- Prefer accessible semantic HTML and static Astro components.
- Run `npm run check`, `npm test`, and `npm run build` before submitting changes.
